fastapi>=0.110,<0.116
uvicorn>=0.31.1,<1
pydantic<3
pydantic-settings>=2.5.2,<3
typing-extensions>=4.13.0
requests>=2.32.3,<2.33
pandas>=2.2.0
numpy>=1.26.4,<3
scikit-learn>=1.4.2
hmmlearn>=0.3.2
joblib>=1.4.0
river>=0.21.0
lz4==4.3.2
xxhash==3.4.1
# Hugging Face libraries serve knowledge-base embeddings and speech recognition.
transformers==5.3.0
sentence-transformers>=3.0.0
huggingface-hub>=0.35.3
tokenizers>=0.21.0
safetensors==0.4.3
pytest==7.4.0
pytest-asyncio==0.21.1
openai>=1.13,<2
python-multipart>=0.0.9,<1
httpx>=0.27.1,<1.0
python-dotenv==1.0.0
aiofiles>=24.1.0,<27
aiohttp>=3.11.9,<4
langgraph>=0.2.0
langchain-core>=0.3.0
# LlamaIndex powers hybrid (vector + BM25) retrieval for the external racing
# knowledge base. Annotation label selection does not use these packages.
# llama-index-core ships the VectorStoreIndex + QueryFusionRetriever;
# the two sibling packages add the BGE embedding wrapper and the BM25
# retriever respectively.
llama-index-core>=0.11.0,<0.13
llama-index-embeddings-huggingface>=0.3.0,<0.5
llama-index-retrievers-bm25>=0.3.0,<0.6

# Voice — Kokoro neural TTS (Phase 2). Apache-2.0, runs on CPU or GPU via ONNX.
# `onnxruntime` (CPU) is in common; nvidia.txt overrides with onnxruntime-gpu.
kokoro-onnx>=0.4.0
onnxruntime>=1.18.0
soundfile>=0.12.1

# Voice — Pipecat pipeline + faster-whisper STT (Phase 3).
# pipecat-ai pulls in its own deps for the silero VAD and websocket transport.
# faster-whisper is the streaming STT backend; runs on CPU or GPU.
pipecat-ai[silero,openai,websocket]>=0.0.50
faster-whisper>=1.0.3
