fastapi>=0.110,<0.116
uvicorn>=0.31.1,<1
pydantic<3
pydantic-settings>=2.5.2,<3
typing-extensions>=4.13.0
requests>=2.32.3,<2.33
pandas>=2.2.0
numpy>=1.26.4,<3
scikit-learn>=1.4.2
hmmlearn>=0.3.2
joblib>=1.4.0
river>=0.21.0
pyarrow>=15.0.2
# Lance is the telemetry store backend. `pylance` ships the Python
# bindings (import name: `lance`). Daft is the DataFrame engine used by
# the cleaning pipeline. The previous Zarr backend was removed once the
# typed-Lance migration completed.
pylance>=0.20.0
getdaft>=0.3.0
lz4==4.3.2
xxhash==3.4.1
transformers==5.3.0
sentence-transformers>=3.0.0
huggingface-hub>=0.35.3
tokenizers>=0.21.0
accelerate>=0.26.1
datasets==2.16.1
peft==0.8.2
sentencepiece==0.2.0
safetensors==0.4.3
tensorboard>=2.18.0,<3
pytest==7.4.0
pytest-asyncio==0.21.1
openai>=1.13,<2
python-multipart>=0.0.9,<1
httpx>=0.27.1,<1.0
plotly==5.17.0
matplotlib>=3.9.0,<4
seaborn==0.13.0
python-dotenv==1.0.0
aiofiles>=24.1.0,<27
aiohttp>=3.11.9,<4
streamlit>=1.41.0
einops
gguf
timm
langgraph>=0.2.0
langchain-core>=0.3.0
claude-agent-sdk>=0.1.0
# claude-agent-sdk's create_sdk_mcp_server returns CallToolResult from its
# in-process call_tool handler. mcp's lowlevel server only short-circuits on
# `isinstance(results, CallToolResult)` starting in 1.19 — older versions
# iterate the pydantic model as (field, value) tuples and fail validation,
# which surfaces as a pydantic error in every tool call.
mcp>=1.19
mistral_common==1.5.5

# Voice — Kokoro neural TTS (Phase 2). Apache-2.0, runs on CPU or GPU via ONNX.
# `onnxruntime` (CPU) is in common; nvidia.txt overrides with onnxruntime-gpu.
kokoro-onnx>=0.4.0
onnxruntime>=1.18.0
soundfile>=0.12.1

# Voice — Pipecat pipeline + faster-whisper STT (Phase 3).
# pipecat-ai pulls in its own deps for the silero VAD and websocket transport.
# faster-whisper is the streaming STT backend; runs on CPU or GPU.
pipecat-ai[silero,openai,websocket]>=0.0.50
faster-whisper>=1.0.3

