# Core dependencies for Electron desktop Python scripts
# Synchronise with speech setup requirements when versions change.
pyaccsharedmemory
pandas==2.1.4
numpy==1.24.4
scikit-learn==1.3.2
pydantic==2.5.0
pydantic-settings==2.1.0
joblib==1.3.2
scipy==1.11.1
SpeechRecognition==3.10.0
# Older PyAudio wheels required a system PortAudio SDK. The 0.2.14 wheels vendor the dependency for Windows.
pyaudio==0.2.14
# pocketsphinx 0.1.x relies on distutils which was removed in Python 3.12. Keep a 5.x release to avoid the legacy build path.
pocketsphinx==5.0.3
soundfile==0.12.1
librosa==0.10.0
noisereduce==3.0.0
webrtcvad==2.0.10
pyannote-audio==3.1.1
openai-whisper==20230314
torch==2.0.1
torchaudio==2.0.2

# Phase 4 — Piper neural TTS for on-track guidance cues.
# Runs as a persistent subprocess (piper_tts_daemon.py); kept-alive to avoid
# the ~1s model-load tax per cue. CPU-friendly, ~30ms synth per short phrase.
piper-tts==1.2.0
huggingface-hub>=0.20.0
