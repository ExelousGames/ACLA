#to update the name or password, must delete db data stored inside folder
MONGO_DATEBASE=ACLA
MONGO_ADMINUSERNAME=root 
MONGO_ADMINPASSWORD=rootpass
MONGO_CLIENTNAME=client
MONGO_CLIENTPASSWORD=clientpass
MONGO_URL=mongodb_c

#must put REACT_APP at the front for react js (some may not required because I redirect them to dockerfile)
BACKEND_SERVER_IP=192.168.50.1
BACKEND_PROXY_PORT=80
BACKEND_PORT=7001

REACT_WEBSITE_PORT=3000

# AI Service Configuration
AI_SERVICE_URL=http://192.168.50.2:8000
BACKEND_SERVER_IP_FOR_AI=192.168.50.1
HF_TRAINING_ENABLED=true
# OPENAI_API_KEY and GEMINI_API_KEY will be loaded from .env.secrets file (not committed to git)

# Google Cloud Platform Configuration (for Gemini Batch API)
GOOGLE_CLOUD_PROJECT=gen-lang-client-0101192773
GEMINI_BATCH_GCS_BUCKET=telemetry_inference_data
# GOOGLE_APPLICATION_CREDENTIALS will be loaded from .env.secrets file

# Debugpy Configuration
#If you want to enable debugpy in the AI service,
